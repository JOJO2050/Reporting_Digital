import 'package:flutter/material.dart';

class pageActualite3 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Page de Destination Actualité 3'),
      ),
      body: const Center(
        child: Text('Bienvenue sur la page de destination  Actualité 3!'),
      ),
    );
  }
}
